import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const Router = () => {
  return (
    <SafeAreaView>
      <Text>Hello</Text>
    </SafeAreaView>
  );
};

export default Router;
